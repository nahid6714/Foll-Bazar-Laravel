<?php
use Illuminate\Database\Migrations\Migration; use Illuminate\Database\Schema\Blueprint; use Illuminate\Support\Facades\Schema;
return new class extends Migration { public function up():void{if(Schema::hasTable('coupons'))return;Schema::create('coupons',function(Blueprint $t){$t->string('id',36)->primary();$t->string('code',100)->unique();$t->enum('discount_type',['percent','fixed']);$t->decimal('discount_value',12,2);$t->decimal('min_order',12,2)->default(0);$t->decimal('max_discount',12,2)->nullable();$t->integer('usage_limit')->nullable();$t->integer('used_count')->default(0);$t->boolean('is_active')->default(true);$t->dateTime('starts_at')->nullable();$t->dateTime('expires_at')->nullable();$t->timestamps();});} public function down():void{/* legacy table preserved intentionally; not dropped on rollback */} };
